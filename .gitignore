/target
**/*.rs.bk
*.pdb
# west workspace: the manifest repo is firmware/, and west clones its
# dependencies as siblings at the repo root.
/.west/
/zephyr/
/modules/
/bootloader/
/tools/
/build/
/build-pico/
/build-pico-mcuboot/
# generated provisioning artefacts
provision.uf2
provision.hex
/firmware/**/build/
/build-pico/
/build-pico-mcuboot/
# generated provisioning artefacts
provision.uf2
provision.hex
# local artefacts
*.bin
*.hex
*.elf
/flash.bin
# ...but the signed test fixtures are source, not artefacts: the e2e tests
# include_bytes! them, so without these a fresh clone does not COMPILE.
!crates/smp-client/tests/fixtures/*.bin

# Local-only: host setup notes specific to one machine (paths, USB topology,
# group membership). Useful to have to hand, not useful to anyone else, and not
# something to publish.
/docs/PREREQUISITES.md
