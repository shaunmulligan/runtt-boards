/target
**/*.rs.bk
*.pdb
# west workspace: the manifest repo is firmware/, and west clones its
# dependencies as siblings at the repo root.
/.west/
/zephyr/
/modules/
/bootloader/
/tools/
/build/
/firmware/**/build/
# local artefacts
*.bin
*.hex
*.elf
/flash.bin

# Local-only: host setup notes specific to one machine (paths, USB topology,
# group membership). Useful to have to hand, not useful to anyone else, and not
# something to publish.
/docs/PREREQUISITES.md
