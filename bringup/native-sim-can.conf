# SMP over CAN on native_sim, against a Linux vcan interface.
CONFIG_RUNTT_SMP_CAN=y
CONFIG_CAN=y
CONFIG_ISOTP=y

# The default is small and this transport hands whole SMP packets around.
CONFIG_ISOTP_RX_BUF_COUNT=8
CONFIG_ISOTP_RX_BUF_SIZE=256
