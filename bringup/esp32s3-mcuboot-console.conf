# MCUboot's own console on UART0 (the CH343 bridge), for bring-up: the bridge
# does not re-enumerate when the target resets, so it witnesses every boot's
# swap decision without racing the USB stack.
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_MCUBOOT_LOG_LEVEL_INF=y
